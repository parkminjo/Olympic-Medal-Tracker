import React, { useState } from "react";

const CountryList = () => {
  const [countryList, setCountryList] = useState([]);

  return (
    <ul>
      <li>안녕하세요</li>
    </ul>
  );
};

export default CountryList;
